# create the list using the [] function
a = [1, 2, 3, 56, 4]
print(a)
# for acessing the particular element. Elements always start from the zero element
# print the list using the print() function

# Acess elements using the index of the list.
print(a[0])

# change the value of the list using
a[0] = 98
print(a)
# we can create the list with the list of different items types
a = [1, False, True, 2, 3.5, "Manan", 'M']
print(a)

# List Slicing
friends = ["Harry", "Manan", "Aman", "Sarthak", "Anu", "Divya"]
print(friends[0:4])
print(friends[-4:])

# creating the tuple using the ()
t=(1,2,3,4,5)
print(t)

# printing the tuple element
print(t[0])

# we  cannot update the values of the tuple as it is not allowed in the tuple
# print(t[2])
# t[2]=90 we cannot update the value in the tuple as it will throw the error

t1=(1) # Wrong ways to intialize the tuple with the single Element
t1=(1,) # Tuple with the single Element
# Lists Method
L1 = [1, 5, 0, 45, 3, 2, 98, 90, 12]
# it will Sort the original list and we can't create the variable to Store the Sorted  List as It will give none in the answer.
# L1.sort()


# L1.reverse() Reverse the lists
# L1.append(45) Append at the end of the List
# L1.append(88)
#L1.insert(0,69) # add the value of the 69 at the zero index position .
#L1.pop(2) # will remove the element in the list at a given index position
# L1.remove(12) # will remove given element in the list .

print(L1)
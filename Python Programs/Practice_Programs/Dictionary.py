import os
# we import the os directory from the inbuild module
print(os.listdir('D:\\Manan Work\\Programming in the Python\\Python Notes'))
# we use the os.listdir() to list the directory of the contents
# \\ is the escape sequence characters

a = input('Enter Your Name: ')
print(a)

b = input('Enter the Number: ')
b = int(b)
# if possible it will typecast the String value into the integer.
# anyValue which is inputted in the String it takes as the String not on the Other datatypes.
print(type(b))